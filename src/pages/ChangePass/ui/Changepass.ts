// const controller = new SettingsController();

import { Block, type defaultProps } from '@/app/utils/Block';

export class Settings extends Block<defaultProps> {
  createResources() {}

  protected render(): HTMLElement | DocumentFragment {}
}
